package help;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import page.HomePage;

public class Open {
    private WebDriver driver;
    public Open(WebDriver driver){ this.driver = driver; }

}
